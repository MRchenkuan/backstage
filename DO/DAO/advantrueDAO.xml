<?xml version="1.0" encoding="UTF-8"?>
<mapper>
    <!-- 根据id更新照片信息 -->
    <update id="updateItemByInfo" >
        update into T_ADVANTCE(#{colname}) VALUES(#{value}) where id="#{id}";
    </update>
</mapper>